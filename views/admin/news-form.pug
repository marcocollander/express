extends  ../layout

block content
	h1= title

	if errors.errors !== undefined
		each message in errors.errors
			p=message

	form(method='post')
		p
			label Tytuł:    
				input(type='text' name='title' value=body.title || '')
		p 
			label Wpis:     
		div 
			textarea(name='description' cols='60' rows='15')=body.description || '' 
		p 
			input(type='submit' value='Zapisz') 


